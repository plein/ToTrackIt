# Grafana dashboard provisioning configuration
apiVersion: 1

providers:
  - name: 'ToTrackIt Dashboards'
    orgId: 1
    folder: 'ToTrackIt'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards